{
  // 全局重试与冷却设置（也可在 TUI 中配置: /auto-router config → ⚙️ 重试与冷却设置）
  // 留空或删除则使用默认值: maxRetries=3, backoffBase=2s, backoffMax=30s, transientCooldown=1m, longCooldown=12h
  "retry": {
    "maxRetries": 4,
    "backoffBaseMs": 2000,
    "backoffMaxMs": 30000,
    "transientCooldownMs": 60000,
    "longCooldownMs": 43200000,
    "retryEmptyResponses": true // 结束检测：响应无任何内容时视为失败并 failover/重试（默认 true）
  },
  "routes": {
    "default": {
      "strategy": "cache-first",
      "targets": [
        { "provider": "ducky", "model": "qwen3.8-max", "weight": 1 },
        { "provider": "ducky", "model": "glm-5.2", "weight": 1 },
        { "provider": "ducky", "model": "deepseek-v4-pro", "weight": 1 }
      ]
    },
    "performance": {
      "strategy": "cache-first",
      "targets": [
        { "provider": "ducky", "model": "claude-opus-4-6", "weight": 2 },
        { "provider": "ducky", "model": "qwen3.8-max", "weight": 1 },
        { "provider": "ducky", "model": "glm-5.2", "weight": 1 }
      ]
    },
    "fast": {
      "strategy": "round-robin",
      "targets": [
        { "provider": "ducky", "model": "glm-5.2" },
        { "provider": "ducky", "model": "deepseek-v4-pro" }
      ]
    }
  },
  "hide": ["anthropic"]
}
